CLEANFILES = *~
MAINTAINERCLEANFILES = Makefile.in

if VISUAL
  VISUALDIR = swe lbm poisson
endif

if QT
  QTDIR = qt
endif

if MPI
  MPIDIR = mpi
endif

SUBDIRS = $(VISUALDIR) $(MPIDIR) $(QTDIR) config
